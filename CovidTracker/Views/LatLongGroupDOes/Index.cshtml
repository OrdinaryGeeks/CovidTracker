@model IEnumerable<CovidTracker.Models.LatLongGroupDO>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NorthWestLat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NorthWestLong)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SouthEastLat)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SouthEastLong)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NorthWestLat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NorthWestLong)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SouthEastLat)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SouthEastLong)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LatLongGroupDOID }) |
            @Html.ActionLink("Details", "Details", new { id=item.LatLongGroupDOID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LatLongGroupDOID })
        </td>
    </tr>
}

</table>
