@model IEnumerable<CovidTracker.Models.TimeBlock>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Begin)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.End)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Begin)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.End)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TimeBlockID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TimeBlockID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TimeBlockID })
        </td>
    </tr>
}

</table>
